# ... revised by Claude Sonnet 4.6
# Replaced StanHeaders/BH with autodiff (header-only, bundled in inst/include/).
# Bumped CXX_STD to CXX17 (autodiff requires C++17).
CXX_STD = CXX17
PKG_CXXFLAGS = $(shell pkg-config --cflags nlopt)

PKG_CPPFLAGS = -I../inst/include \
               -I$(shell pkg-config --variable=includedir nlopt)

PKG_LIBS = $(shell pkg-config --libs nlopt)
